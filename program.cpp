#include"grammer.h"
#include"xcs.h"
#include"function.h"
#include"normal.h"
#include"compile.h"
#include"file.h"
using namespace std;
int main(void){
ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
long long a=0;
runtime r;
r.start();
formem(10000000000)
a+=1;
outf(str(a));
outf(str(r.finish()));
}
